import { createContext, useState } from 'react';

/** This context provides access to the shopping cart to all pages. */

const ShoppingCartContext = createContext({});
export default ShoppingCartContext;