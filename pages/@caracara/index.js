/* This is a stub Component that represents @caracara's
   profile page.

   We could do a fully custom profile page if we so chose,
   but to keep things simple for this assigment we just use
   the template ProfileLayout. */

   const ProfilePage = (props) => {}
   ProfilePage.layout = 'ProfileLayout';
   
   export default ProfilePage;